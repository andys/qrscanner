require 'echoe'

Echoe.new("qrscanner") do |p|
  p.author = "Andrew Snow"
  p.email = 'andrew@modulus.org'
  p.summary = "Ruby gem QR code scanner based on ZXing"
  p.url = "http://github.com/andys/qrscanner"
  p.ignore_pattern = /^pkg|^doc|^\.svn|^\.git/
end
